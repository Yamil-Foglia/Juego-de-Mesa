// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

export const firebaseConfig = {
  apiKey: "AIzaSyB3mUxCSAnVjvOumo3312gSyOP8VnEzuII",
  authDomain: "gameoftable-4ed8f.firebaseapp.com",
  databaseURL: "https://gameoftable-4ed8f.firebaseio.com",
  projectId: "gameoftable-4ed8f",
  storageBucket: "gameoftable-4ed8f.appspot.com",
  messagingSenderId: "41272379777",
  appId: "1:41272379777:web:907505cde50f5438630080"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
